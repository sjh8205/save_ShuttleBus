<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sonhyejin.save_shuttlebus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".T_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".P_main" />
        <activity android:name=".H_main" />
        <activity android:name=".H_View" />
        <activity android:name=".H_Register" />
        <activity android:name=".Login" />
        <activity android:name=".T_QRScan" />
        <activity android:name=".H_Join"></activity>
    </application>

</manifest>